node {
    def branch = params.BRANCH
    stage('Foundation') {
        build job: 'foundation', parameters: [string(name: 'BRANCH', value: branch)]
    }
    stage('Core') {
        build job: 'core', parameters: [string(name: 'BRANCH', value: branch)]
    }
}